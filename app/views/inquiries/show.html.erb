<div id='records'>
  <h2><%= t('.inquiry')%></h2>
  <table id='inquiry'>
    <tr>
      <td>
        <strong><%= t('.from') %></strong>
      </td>
      <td>
        <%= @inquiry.name %>
      </td>
    </tr>
    <tr>
      <td>
        <strong><%= t('.date') %></strong>
      </td>
      <td>
        <%= l(Date.parse(@inquiry.created_at.to_s), :format => :long) %>
      </td>
    </tr>
    <tr>
      <td valign='top'>
        <strong><%= t('.message') %></strong>
      </td>
      <td>
        <%= simple_format strip_tags(@inquiry.message), :style => 'margin-top: 0' %>
      </td>
    </tr>
    <tr>
      <td>
        <strong><%= t('.answer') %></strong>
      </td>
      <td>
        <%= simple_format strip_tags(@inquiry.answer), :style => 'margin-top: 0' %>
      </td>
    </tr>
    <tr>
      <td>
        <strong><%= t('.answered_by') %></strong>
      </td>
      <td>
        <%= @inquiry.recipient %>, <%= l(Date.parse(@inquiry.created_at.to_s), :format => :long) %>
      </td>
    </tr>
  </table>
</div>
